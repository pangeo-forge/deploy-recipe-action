name: Test action

on:
  push:
    branches: ["main"]
  pull_request:
    branches: [ "*" ]

jobs:
  test:
    name: (recipes@${{ matrix.recipes-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        recipes-version: ["0.10.3"]  # , "latest", "main"]
    steps:
      - name: "Clone test feedstock"
        run: |
          git clone "git+https://github.com/pangeo-forge/pangeo-forge-recipes.git@{{ matrix.recipes-version}}"
      - name: "Deploy recipes"
        uses: "pangeo-forge/deploy-recipe-action@${{ github.head_ref }}"
        with:
          # select_recipe_by_label: true
          pangeo_forge_runner_config: >
            {
            "BaseCommand": {
              "feedstock_subdir": "pangeo-forge-recipes/examples/feedstock"
            },
            "Bake": {
              "bakery_class": "pangeo_forge_runner.bakery.local.LocalDirectBakery"
            },
            "TargetStorage": {
              "fsspec_class": "fsspec.implementations.local.LocalFileSystem",
              "root_path": "./target"
            },
            "InputCacheStorage": {
              "fsspec_class": "fsspec.implementations.local.LocalFileSystem",
              "root_path": "./cache"
            }
            }
